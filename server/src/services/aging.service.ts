// TODO: Aging calculation service
// calculateDueDate(invoiceDate: Date, paymentTerms: number): Date
// getAgingData(invoices): AgingRow[]
// Vendor due date = invoice_date + vendor.payment_terms (days)
// Overdue = today > due_date AND balance > 0
