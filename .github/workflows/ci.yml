name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  typecheck-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            server/package-lock.json
            client/package-lock.json

      - name: Install server dependencies
        run: cd server && npm ci

      - name: Install client dependencies
        run: cd client && npm ci

      - name: Typecheck server
        run: cd server && npx tsc --noEmit

      - name: Typecheck client
        run: cd client && npx tsc --noEmit

      - name: Run server tests
        # Integration tests need a live server + Postgres which CI doesn't
        # spin up — they're meant for local runs (`npm run dev:server` first).
        # Tracked: real CI integration coverage in a separate PR.
        run: cd server && npx vitest run --exclude '**/integration.test.ts'

  build:
    runs-on: ubuntu-latest
    needs: typecheck-and-test
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            server/package-lock.json
            client/package-lock.json

      - name: Install & build server
        run: cd server && npm ci && npm run build

      - name: Install & build client
        run: cd client && npm ci && npm run build
