// TODO: formatters
// fmtINR(value)  — formats number as ₹X,XX,XXX (en-IN locale)
// fmtDate(date)  — formats ISO date string as "14 Nov 2025"
// fmtSize(bytes) — formats bytes as "245 KB" or "1.2 MB"
